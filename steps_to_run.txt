1. Open 2 terminals

2. cd to custom algo folder in both terminals

3. Run source venv/bin/activate in both terminals

4. In terminal 1: run ps auxww | grep 'celery worker' | awk '{print $2}' | xargs kill -9

5. In terminal 1: run celery worker -A tasks &

6. In terminal 2: run python3 controller.py


To access DB:

1. mysql -u scox -p
2. enter pw (hint 4 chars)
3. use trading_algo